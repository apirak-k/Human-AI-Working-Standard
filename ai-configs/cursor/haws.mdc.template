---
description: HAWS adapter; load canonical rules from the HAWS checkout
globs: *
alwaysApply: true
---

Load canonical standards from `core/HAWS.md` and `core/WORK_INSTRUCTIONS.md`.
Load private preferences from `secondbrain/USER_PREFERENCES.md` and
`secondbrain/ANTI_PATTERNS.md`. This adapter contains no duplicated HAWS rules.
